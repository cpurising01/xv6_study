commit ea962686f39b112841c11489bab2b21449d04357
Author: CPU-rising <cpu@rising.com>
Date:   Sat Jul 25 15:28:44 2026 +0800

    Finish Lab 07 net

diff --git a/Makefile b/Makefile
index 7a7e380..2fbe8b0 100644
--- a/Makefile
+++ b/Makefile
@@ -86,7 +86,7 @@ LD = $(TOOLPREFIX)ld
 OBJCOPY = $(TOOLPREFIX)objcopy
 OBJDUMP = $(TOOLPREFIX)objdump
 
-CFLAGS = -Wall -Werror -O -fno-omit-frame-pointer -ggdb
+CFLAGS = -Wall -Werror -O -fno-omit-frame-pointer -ggdb -Wno-infinite-recursion
 
 ifdef LAB
 LABUPPER = $(shell echo $(LAB) | tr a-z A-Z)
diff --git a/kernel/e1000.c b/kernel/e1000.c
index 70a2adf..4e2c3bd 100644
--- a/kernel/e1000.c
+++ b/kernel/e1000.c
@@ -92,29 +92,49 @@ e1000_init(uint32 *xregs)
   regs[E1000_IMS] = (1 << 7); // RXDW -- Receiver Descriptor Write Back
 }
 
+#define E1000_TXD_CMD_IFCS 0x02
+
 int
 e1000_transmit(struct mbuf *m)
 {
-  //
-  // Your code here.
-  //
-  // the mbuf contains an ethernet frame; program it into
-  // the TX descriptor ring so that the e1000 sends it. Stash
-  // a pointer so that it can be freed after sending.
-  //
-  
+  acquire(&e1000_lock);
+  uint32 index = regs[E1000_TDT];
+  if(!(tx_ring[index].status & E1000_TXD_STAT_DD)){
+    release(&e1000_lock);
+    return -1;
+  }
+  if(tx_mbufs[index]){
+    mbuffree(tx_mbufs[index]);
+  }
+  tx_ring[index].addr = (uint64)m->head;
+  tx_ring[index].length = (uint16)m->len;
+  tx_ring[index].cmd = E1000_TXD_CMD_EOP | E1000_TXD_CMD_RS;
+  tx_mbufs[index] = m;
+  regs[E1000_TDT] = (index+1) % TX_RING_SIZE;
+  release(&e1000_lock);
   return 0;
 }
 
 static void
 e1000_recv(void)
 {
-  //
-  // Your code here.
-  //
-  // Check for packets that have arrived from the e1000
-  // Create and deliver an mbuf for each packet (using net_rx()).
-  //
+  while(1){
+    uint32 index = (regs[E1000_RDT] + 1) % RX_RING_SIZE;
+    if(!(rx_ring[index].status & E1000_RXD_STAT_DD)){
+      return;
+    }
+    struct mbuf *m = rx_mbufs[index];
+    m->len = rx_ring[index].length;
+    net_rx(m);
+    
+    rx_mbufs[index] = mbufalloc(0);
+    if (!rx_mbufs[index]) {
+      panic("e1000_recv");
+    }
+    rx_ring[index].addr = (uint64)rx_mbufs[index]->head;
+    rx_ring[index].status = 0;
+    regs[E1000_RDT] = index;
+  }
 }
 
 void
diff --git a/packets.pcap b/packets.pcap
index 82d353e..402a3c2 100644
Binary files a/packets.pcap and b/packets.pcap differ
diff --git a/time.txt b/time.txt
new file mode 100644
index 0000000..00750ed
--- /dev/null
+++ b/time.txt
@@ -0,0 +1 @@
+3
